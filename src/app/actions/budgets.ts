"use server";
import { revalidatePath } from "next/cache";
import { prisma } from "@/lib/prisma"; import { requireUserId } from "@/lib/get-user";
import { budgetSchema, emptyToUndefined, firstError, type ActionResult } from "@/lib/validations";
import { getActionWorkspace, scopeWorkspace } from "@/lib/workspace";
export async function createBudget(_p: ActionResult, f: FormData): Promise<ActionResult> { const userId=await requireUserId(); const workspaceId=await getActionWorkspace(userId,String(f.get("workspaceId")||"")); const p=budgetSchema.safeParse({categoryId:f.get("categoryId"),amount:f.get("amount"),currency:emptyToUndefined(f.get("currency"))??"MXN",month:f.get("month")}); if(!p.success)return{ok:false,error:firstError(p.error)}; if(!(await prisma.category.findFirst({where:{id:p.data.categoryId,...scopeWorkspace(userId,workspaceId)}})))return{ok:false,error:"Categoría inválida"}; try{await prisma.budget.create({data:{userId,workspaceId,...p.data}})}catch{return{ok:false,error:"Ya existe ese presupuesto"}} revalidatePath("/presupuestos"); revalidatePath("/"); return{ok:true}; }
export async function deleteBudget(id:string, requestedWorkspaceId?:string){const userId=await requireUserId();const workspaceId=await getActionWorkspace(userId,requestedWorkspaceId);await prisma.budget.deleteMany({where:{id,...scopeWorkspace(userId,workspaceId)}});revalidatePath("/presupuestos");revalidatePath("/");}
