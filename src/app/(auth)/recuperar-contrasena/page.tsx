"use client";
import Link from "next/link";
import { useActionState } from "react";
import { requestPasswordReset } from "../reset-actions";
import { Card, FormError, btnPrimaryCls, inputCls, labelCls, linkCls } from "@/components/ui";
export default function ForgotPasswordPage() { const [state, action, pending] = useActionState(requestPasswordReset, { ok: true }); return <main className="mx-auto flex min-h-screen w-full max-w-sm flex-col justify-center p-4"><Card><h1 className="mb-1 text-2xl font-bold">Restablecer contraseña</h1><p className="mb-6 text-sm text-zinc-600">Te enviaremos un enlace si el correo está registrado.</p><form action={action} className="grid gap-4"><FormError message={state.ok ? null : state.error} />{state.ok && state.message && <p className="rounded-lg bg-emerald-50 px-3 py-2 text-sm text-emerald-700">{state.message}</p>}<div><label className={labelCls} htmlFor="email">Correo</label><input id="email" name="email" type="email" autoComplete="email" className={inputCls} required /></div><button className={btnPrimaryCls} disabled={pending}>{pending ? "Enviando..." : "Enviar enlace"}</button></form><p className="mt-4 text-center text-sm"><Link href="/login" className={linkCls}>Volver a iniciar sesión</Link></p></Card></main>; }
