# Commands
- Install: `corepack pnpm install`
- Lint: `corepack pnpm lint`
- Build: `corepack pnpm build`
- Dev: `corepack pnpm dev`
- Prisma generate: `corepack pnpm db:generate`
- If pnpm is not on PATH, `npm run build` can invoke Next scripts when dependencies are installed.